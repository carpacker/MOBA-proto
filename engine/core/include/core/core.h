#pragma once
// eng_core — dependency-free leaf. Real contents (arenas, containers, handle.h,
// sim_config.h, log/assert, test.h) arrive in Phase 1. Placeholder for the M0.2 spine.

const char* eng_core_version(void);
