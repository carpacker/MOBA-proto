#include "render/render.h"
#include "core/core.h"          // compile-time proof the core/math/platform include
#include "math/math.h"          // graph propagates through the link targets
#include "platform/platform.h"

const char* eng_render_version(void) { return "eng_render 0.0.0 (spine)"; }
