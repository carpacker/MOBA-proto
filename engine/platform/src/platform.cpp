#include "platform/platform.h"
#include "core/core.h"  // compile-time proof eng::core's PUBLIC include dir propagates

const char* eng_platform_version(void) { return "eng_platform 0.0.0 (spine)"; }
